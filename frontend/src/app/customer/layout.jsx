"use client";
import VoiceLayer from "@/app/components/VoiceLayer";

export default function CustomerLayout({ children }) {
    return (
        <VoiceLayer>
            {children}
        </VoiceLayer>
    );
} 